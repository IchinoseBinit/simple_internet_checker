name: internet_checker
description: A new package check to the internet connection of a app.
repository: https://github.com/IchinoseBinit/internet_checker
homepage: https://binitkoirala.com.np

version: 0.0.1

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: '>=2.5.0'

dependencies:
  flutter:
    sdk: flutter

  connectivity_plus: ^2.2.1
  provider: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
